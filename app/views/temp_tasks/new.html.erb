<div class="container">
  <div class="row">
    <div class="col-sm-4">
      <%= simple_form_for @temp do |f| %>
        <%= f.input :companys, hint: 'Вводим компании используя символ ";"' %>
        <%= f.select :author_id, Author.all.map{ |l| [l.name, l.id] }  %>
        <%= f.input :date_start, as: :date %>
        <%= f.input :date_end, as: :date %>   
        <%= f.submit 'Добавить' %>
      <% end %>
    </div>
  </div>
</div>